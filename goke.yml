global:
  environment:
    MY_VAR: "my var value"
    MY_VAL: "$(ls)"
    CSS_FILES: "styles/*.css"

  events:
    before_each_run:
      - "echo 'before each 1'"
      - "echo 'before each 2'"
    after_each_run:
      - "echo 'after each 1'"
      - "greet-lisha"
    # before_each_task:
    #   - "echo 'before task'"
    # after_each_task:
    #   - "echo 'after task'"

build-go: 
  files: [main.go, events.go, cmd/cli/*.go]
  run:
    - "go run $(files)"

build-css:
  files: [$(CSS_FILES)]
  run:
    - "ls -la"

build-all:
  run:
    - "this.build-go"
    - "build-css"

greet-lisha:
  run:
    - "echo 'Hello Lisha!'"

greet-loki:
  run:
    - 'echo "Hello Boki"'

greet-cats:
  run:
    - 'echo "Hello Frey"'
    - 'echo "Hello Sunny"'
    - "greet-loki"
